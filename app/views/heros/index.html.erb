<div class="container">
  <div class="sidebar">
    <!-- must display the search form -->
    <!-- must allow for a search edit and update -->
  </div>
</div>

  <!-- container that loops over the heros list and displays query results -->
<div class="container">
  <div class="heros-list">
  <% @heros.each do |hero| %>
    <div class="row">
      <div class="col-xs-12 col-sm-3">
        <h3>Profile Pictures</h3>
      </div>
      <div class="col-xs-12 col-sm-9">
        <h3>Description</h3>
      </div>
    </div>
  <% end %>
  </div>
</div>

